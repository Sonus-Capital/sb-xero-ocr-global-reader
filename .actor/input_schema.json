{
  "title": "SB Xero OCR Global Reader",
  "description": "Reads a single Xero attachment payload (stringified JSON) and performs PDF text extraction.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "json": {
      "title": "Attachment JSON payload",
      "type": "string",
      "description": "Stringified JSON with the attachment metadata and fileContentBase64.",
      "editor": "textarea"
    }
  },
  "required": ["json"]
}
