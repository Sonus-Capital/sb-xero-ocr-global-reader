{
  "title": "SB Xero OCR Global Reader",
  "description": "Reads a single Xero attachment PDF and returns OCR plus metadata.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "specification": {
      "title": "Specification",
      "description": "Payload from Make containing the file and all related metadata.",
      "type": "object",
      "editor": "json"
    }
  },
  "required": ["specification"]
}
