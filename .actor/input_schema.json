{
  "title": "SB Xero OCR Global Reader",
  "description": "Reads a single Xero attachment payload and extracts PDF text. Input is passed through from Make as-is.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "json": {
      "title": "Optional debug json (unused)",
      "type": "string",
      "description": "Optional raw JSON string for debugging. Not required by the actor.",
      "editor": "textarea"
    }
  },
  "required": [],
  "additionalProperties": true
}
